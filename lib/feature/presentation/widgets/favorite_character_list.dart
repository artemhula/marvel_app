import 'package:flutter/material.dart';

class FavoriteCharacterList extends StatelessWidget {
  const FavoriteCharacterList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorite List'));
  }
}
